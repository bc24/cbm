"""
Aufgabe 1
Schreibe ein Programm welches sich eine Zufallszahl erzeugt (Runde auf 2 Nachkommastellen).
Verlange, dass der Benutzer eine Zahl eingibt. Liegt die eingegebene Zahl unter der
Zufallszahl, soll das Programm den Satz "weniger" ausgeben. (Liegt diese höher, dann "mehr").
Hat der Benutzer die "richtige" Zahl erraten, gebe eine Bestätigung aus
"""


"""
Erstelle ein Programm. Verwende den Code aus der Häufigkeitsermittlung und schreibe
die Ergebnisse in eine ".csv" Datei. In der Datei muss der Buchstabe und die Häufigkeiten.
Achtet darauf, nur Zeichen und Ziffern zu nutzen. (Verändere die Berechnung der Häufigkeiten,
damit die Werte mit denen aus der ".csv" Datei übereinstimmen)
"""

"""
3)	Öffne die Datei, welche in Aufgabe 2 erstellt wurde. Verwende als Modus "read"
Nimm diesen Code als Beispiel:

f = open(DATEINAME, 'r')
lines = r.readlines()
f.close()

Der Inhalt der Datei befindet sich jetzt in der Liste "lines"
Versuche jetzt, die relative Häufigkeit aus den .csv  Werten zu summieren.
Gebe die Summe am Ende aus.
"""
